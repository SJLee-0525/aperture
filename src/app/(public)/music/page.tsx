import { ComingSoon } from "@/features/coming-soon/ComingSoon";

/** 임시 — Phase B에서 getMusic*() + <MusicView/> 로 교체. */
export default function MusicPage() {
  return <ComingSoon titleKey="sectionMusic" />;
}
