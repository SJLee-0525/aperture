/**
 * 대표 이미지가 있는 블로그 상세 히어로의 최소 높이(px).
 *
 * 사진이 배경으로 읽히려면 어느 정도 면적이 필요해 앨범 상세와 같은 값을 쓴다. 내용이 길면
 * 그만큼 늘어나므로 위쪽에 빈 띠가 남지 않는다. 대표 이미지가 없는 글은 이 값을 쓰지 않고
 * 글 높이에 맞춘다 — 채울 사진이 없는데 자리만 비워 두면 제목이 화면 밖으로 밀린다.
 *
 * 대기 화면 스켈레톤이 같은 값을 써야 본문이 들어올 때 첫 화면이 흔들리지 않는다.
 */
const ARTICLE_HERO_MIN_HEIGHT = 300;

export { ARTICLE_HERO_MIN_HEIGHT };
