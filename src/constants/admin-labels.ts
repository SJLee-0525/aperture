/**
 * 관리자 화면이 값 없는 이름 자리에 쓰는 문구.
 *
 * 목록 행과 삭제 확인이 같은 문자열을 써야 관리자가 같은 항목임을 알아본다.
 * 일곱 파일에 리터럴로 흩어져 있던 값이다.
 */
const ADMIN_UNTITLED = "제목 없음";

/** 제목 대신 이름을 갖는 항목(수상)이 쓴다. */
const ADMIN_UNNAMED = "이름 없음";

export { ADMIN_UNNAMED, ADMIN_UNTITLED };
